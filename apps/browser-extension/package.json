{
  "name": "latexsnipper-browser",
  "private": true,
  "version": "0.1.0",
  "description": "LaTeXSnipper Browser Extension",
  "scripts": {
    "build": "vite build",
    "build:chrome": "vite build --mode chrome",
    "build:firefox": "vite build --mode firefox",
    "typecheck": "tsc --noEmit",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "@types/chrome": "^0.0.300"
  }
}
