# Custom selectors

Selectors are CSS-only, maximum 512 characters each, syntax-validated before storage, and scoped per origin. Password/token/private-key targets, JavaScript expressions, XPath, and remote code are rejected. The element picker uses an isolated overlay, supports Escape cancellation, never includes sensitive text in generated selectors, and removes all page modifications on completion.
